name: mediaexplant
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.2



dev_dependencies:
  flutter_test:
    sdk: flutter  
  flutter_lints: ^2.0.0