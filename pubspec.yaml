name: mediaexplant
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.2
  http: ^0.13.5
  dio: ^5.4.0
  url_launcher: ^6.1.7
  
dev_dependencies:
  flutter_test:
    sdk: flutter  
  flutter_lints: ^2.0.0